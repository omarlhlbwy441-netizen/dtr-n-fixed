apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rafeeq

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

images:
  - name: ghcr.io/omarlhlbwy441-netizen/dtr-n-fixed
    newTag: main
