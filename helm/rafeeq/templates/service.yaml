apiVersion: v1
kind: Service
metadata:
  name: {{ include "rafeeq.name" . }}
  labels:
    {{- include "rafeeq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rafeeq.selectorLabels" . | nindent 4 }}
